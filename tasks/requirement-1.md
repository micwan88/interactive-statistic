---
date: 2026-06-06
---

1. Setting up the site environment and folder structures
2. Making the site style and design and making corresponding config files
3. Migrate `assets/jsx/binomial-normal.jsx` interactives into github page and translate the page content to `English` only
4. Migrate `assets/jsx/clt.jsx` interactives into github page and translate the page content to `English` only
5. Migrate `assets/jsx/r-square.jsx` interactives into github page and translate the page content to `English` only
6. Making a landing page so that it can navigate to corresponding interactives
7. Ensure those migrated pages are using the layout, style, design in step (2)

